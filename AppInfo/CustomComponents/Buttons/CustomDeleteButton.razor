@inherits BaseButtonComponent

<ButtonComponent Caption="@(Caption ?? "Delete")"
                 Color="@(Color ?? "danger")"
                 Icon="@(Icon ?? "trash-o")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Delete It")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />